{% extends 'base.html.twig' %}

{% block title %}HardSkills index{% endblock %}

{% block body %}
    <h1>HardSkills index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Nom</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for hard_skill in hard_skills %}
            <tr>
                <td>{{ hard_skill.nom }}</td>
                <td>
                    <a href="{{ path('app_hard_skills_show', {'id': hard_skill.id}) }}">show</a>
                    <a href="{{ path('app_hard_skills_edit', {'id': hard_skill.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_hard_skills_new') }}">Create new</a>
{% endblock %}
