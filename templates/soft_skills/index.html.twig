{% extends 'base.html.twig' %}

{% block title %}SoftSkills index{% endblock %}

{% block body %}
    <h1>SoftSkills index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Nom</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for soft_skill in soft_skills %}
            <tr>
                <td>{{ soft_skill.nom }}</td>
                <td>
                    <a href="{{ path('app_soft_skills_show', {'id': soft_skill.id}) }}">show</a>
                    <a href="{{ path('app_soft_skills_edit', {'id': soft_skill.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_soft_skills_new') }}">Create new</a>
{% endblock %}
